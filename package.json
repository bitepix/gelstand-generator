{
  "name": "gelstand-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "check:state": "node scripts/check-state.mjs",
    "check:export": "node scripts/check-export.mjs",
    "check:geometry": "node scripts/check-geometry.mjs"
  },
  "dependencies": {
    "manifold-3d": "^2.5.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
