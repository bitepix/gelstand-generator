// Константы геометрии. ТЗ v3, таблица 12.2.
// Все значения в миллиметрах, углы фасок — 45°.
// В интерфейс не выводятся.

/** Толщина стенки плитки. Две соседние дают перегородку 3,0. */
export const WALL = 1.5

/** Толщина основания. */
export const BASE = 1.2

/** Полная высота модели. */
export const HEIGHT = 15.0

/** Плечо угловой стойки вдоль X. */
export const ARM_X = 5.0

/** Плечо угловой стойки вдоль Y. */
export const ARM_Y = 7.0

/** Фаска у подошвы стойки. */
export const CHAMFER_FOOT = 0.75

/** Фаска сверху стоек, с Z = 14,5. */
export const CHAMFER_TOP = 0.5

/** Фаска снизу проёма в основании. */
export const CHAMFER_OPENING = 1.25

/** Скругление выпуклых вертикальных рёбер. */
export const FILLET_CONVEX = 1.0

/** Скругление вогнутых вертикальных рёбер. */
export const FILLET_CONCAVE = 0.5

/** Прирост шага сетки к размеру полости: 2 × WALL. ТЗ 12.3. */
export const PITCH_EXTRA = 2 * WALL

// Границы ввода. ТЗ 5 и 7.

/** Минимальный размер полости, мм. */
export const SIZE_MIN = 10

/**
 * Минимальная глубина полости, мм. Больше общего минимума: проём основания
 * идёт от ARM_Y до D − ARM_Y, и при глубине меньше 12 он вырождается,
 * а плечи стоек соседних плиток налезают друг на друга (ТЗ 5.1 и 12.3).
 */
export const DEPTH_MIN = 12

/** Максимальный размер полости, мм. */
export const SIZE_MAX = 100

/** Знаков после запятой в размере. */
export const SIZE_DECIMALS = 2

/** Минимальное количество ячеек по оси. */
export const COUNT_MIN = 1

/** Предельный габарит модели по каждой оси, мм. */
export const OVERALL_MAX = 500

/** Тайм-аут генерации, мс. ТЗ 16, ERR-10. */
export const GENERATION_TIMEOUT = 15000
